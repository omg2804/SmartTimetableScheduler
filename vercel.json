{
  "version": 2,
  "builds": [
    {
      "src": "newer/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    },
    {
      "src": "api/**/*.js",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/embed",
      "dest": "/newer/build/index.html"
    },
    {
      "src": "/(.*)",
      "dest": "/newer/build/$1"
    }
  ]
}
